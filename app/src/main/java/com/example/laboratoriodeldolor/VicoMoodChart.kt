package com.example.laboratoriodeldolor

// Stubbed placeholder to avoid leftover references from an unfinished Vico migration.
// If you intend to add a Vico-based chart, add it in this file and ensure the
// corresponding Gradle dependency versions match the code. For now, keep this
// file empty of any Vico imports so the project can compile.

// Intentionally empty: no classes or imports.
